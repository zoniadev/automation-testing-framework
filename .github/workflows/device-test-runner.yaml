name: Device Test Runner

on:
  workflow_call:
    inputs:
      device:
        required: true
        type: string
      tags:
        required: false
        type: string
        default: ''
      browser:
        required: false
        type: string
        default: 'chromium'
      environment:
        required: false
        type: string
        default: 'staging'
      report_scope:
        required: false
        type: string
        default: ''
      report_name:
        required: false
        type: string
        default: ''
    secrets:
      SMTP_USER_NASKO:
        required: true
      SMTP_PASS_NASKO:
        required: true
      SMTP_HOST_NASKO:
        required: true
      SMTP_PORT_NASKO:
        required: true

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DEVICE: ${{ inputs.device }}
      REPORT_SCOPE: ${{ inputs.report_scope != '' && inputs.report_scope || inputs.device }}
      REPORT_NAME: ${{ inputs.report_name != '' && inputs.report_name || (inputs.report_scope != '' && inputs.report_scope || inputs.device) }}
      REPORT_BASE_URL: https://zoniadev.github.io/automation-testing-framework
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          npm install -g allure-commandline --save-dev
          npm install nodemailer --save-dev

      - name: Install missing libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwoff1 \
            libevent-2.1-7 \
            libopus0 \
            libgstreamer-plugins-base1.0-0 \
            libgstreamer1.0-0 \
            libharfbuzz-icu0 \
            libhyphen0 \
            libmanette-0.2-0 \
            libflite1 \
            libgles2

      - name: Install Google Chrome (if specified)
        if: inputs.browser == 'chrome'
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          playwright install # Install Playwright dependencies including ffmpeg

      - name: Install Chromium browser for Playwright
        if: inputs.browser != 'chrome'
        run: |
          playwright install chromium

      - name: Run tests
        run: |
          set -euo pipefail
          DEVICE="${{ inputs.device }}"
          TAGS_INPUT="${{ inputs.tags }}"
          BROWSER="${{ inputs.browser }}"
          ENVIRONMENT="${{ inputs.environment }}"
          mkdir -p "allure-results/$REPORT_SCOPE"
          if [ -n "$TAGS_INPUT" ]; then
            TAGS_INPUT=$(echo "$TAGS_INPUT" | tr ' ' ',' | sed 's/,\{2,\}/,/g' | sed 's/^,//;s/,$//')
            TAGS="--tags $TAGS_INPUT"
          else
            TAGS=""
          fi
          echo "Running tests with tags: ${TAGS:-<none>}, device: $DEVICE on env: $ENVIRONMENT with browser: $BROWSER"
          behave $TAGS -D headless=True -D device=$DEVICE -D browser=$BROWSER -D env=$ENVIRONMENT -f allure_behave.formatter:AllureFormatter -o "allure-results/$REPORT_SCOPE" | tee test-summary.txt
        shell: bash

      - name: Copy environment properties
        if: always()
        run: |
          set -euo pipefail
          RESULTS_DIR="allure-results/$REPORT_SCOPE"
          if [ -f "allure-results/environment.properties" ]; then
            cp "allure-results/environment.properties" "$RESULTS_DIR/environment.properties"
          fi
        shell: bash

      - name: Load test report history
        uses: actions/checkout@v4
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages

      - name: Prepare Allure workspace
        if: always()
        run: |
          set -euo pipefail
          DEVICE="${{ inputs.device }}"
          mkdir -p allure-history
          if [ -d gh-pages ]; then
            rsync -a --exclude '.git' gh-pages/ allure-history/
          fi
            mkdir -p "allure-history/$REPORT_SCOPE/$REPORT_NAME"
        shell: bash

      - name: Clean up old reports
        if: always()
        run: |
          set -euo pipefail
          HISTORY_ROOT="allure-history/$REPORT_SCOPE/$REPORT_NAME"
          # Keep at least the last 10 reports, regardless of age
          KEEP_LAST=10
          # Beyond that floor, also drop anything older than 30 days
          MAX_AGE_DAYS=30
          # ...but never prune below this many reports, even if they're stale
          # (keeps sparsely-run scopes from losing their whole trend baseline)
          MIN_KEEP=3

          if [ ! -d "$HISTORY_ROOT" ]; then
            echo "No history root found, skipping cleanup."
            exit 0
          fi

          # Find all numbered directories, ensuring it handles no-matches gracefully
          DIRS=$(find "$HISTORY_ROOT" -mindepth 1 -maxdepth 1 -type d -name '[0-9]*' | sort -V)

          # Handle case where no directories are found
          if [ -z "$DIRS" ]; then
            echo "Found 0 historical reports. No cleanup needed."
            exit 0
          fi

          # Count how many directories we have
          COUNT=$(echo "$DIRS" | wc -l)

          if [ "$COUNT" -gt "$KEEP_LAST" ]; then
            # Determine how many directories to delete
            DELETE_COUNT=$((COUNT - KEEP_LAST))

            # Get the list of directories to delete (the oldest ones)
            DELETE_DIRS=$(echo "$DIRS" | head -n "$DELETE_COUNT")

            echo "Found $COUNT total reports. Deleting the oldest $DELETE_COUNT to keep the last $KEEP_LAST."
            echo "The following report directories will be deleted:"
            echo "$DELETE_DIRS" | while IFS= read -r dir; do echo "  - $dir"; done

            echo "$DELETE_DIRS" | xargs rm -rf
          else
            echo "Found $COUNT reports, which is less than or equal to the retention number of $KEEP_LAST. Skipping count-based cleanup."
          fi

          # Age-based cleanup: among whatever remains, drop reports older than
          # MAX_AGE_DAYS, but always keep at least MIN_KEEP of the newest ones.
          REMAINING=$(find "$HISTORY_ROOT" -mindepth 1 -maxdepth 1 -type d -name '[0-9]*' | sort -V)
          REMAINING_COUNT=$(echo "$REMAINING" | grep -c . || true)

          if [ "$REMAINING_COUNT" -gt "$MIN_KEEP" ]; then
            PROTECTED=$(echo "$REMAINING" | tail -n "$MIN_KEEP")
            CANDIDATES=$(echo "$REMAINING" | head -n "$((REMAINING_COUNT - MIN_KEEP))")

            STALE_DIRS=""
            while IFS= read -r dir; do
              [ -z "$dir" ] && continue
              if [ -n "$(find "$dir" -maxdepth 0 -mtime +"$MAX_AGE_DAYS")" ]; then
                STALE_DIRS="$STALE_DIRS$dir"$'\n'
              fi
            done <<< "$CANDIDATES"

            if [ -n "$STALE_DIRS" ]; then
              echo "Deleting reports older than $MAX_AGE_DAYS days (keeping at least the newest $MIN_KEEP: $(echo "$PROTECTED" | tr '\n' ' ')):"
              echo "$STALE_DIRS" | while IFS= read -r dir; do [ -n "$dir" ] && echo "  - $dir"; done
              echo "$STALE_DIRS" | xargs -r rm -rf
            else
              echo "No reports older than $MAX_AGE_DAYS days found among prunable candidates."
            fi
          else
            echo "Only $REMAINING_COUNT report(s) remain, at or below the minimum floor of $MIN_KEEP. Skipping age-based cleanup."
          fi

          echo "Cleanup complete."
        shell: bash

      - name: Determine report sequence
        if: always()
        run: |
          set -euo pipefail
            REPORT_SCOPE="${REPORT_SCOPE}"
            REPORT_NAME="${REPORT_NAME}"
            HISTORY_ROOT="allure-history/$REPORT_SCOPE/$REPORT_NAME"
            SEQUENCE_FILE="$HISTORY_ROOT/.sequence"

            mkdir -p "$HISTORY_ROOT"

            LAST_SEQUENCE=0
            if [ -f "$SEQUENCE_FILE" ]; then
              VALUE=$(head -n 1 "$SEQUENCE_FILE" | tr -dc '0-9')
              if [ -n "$VALUE" ]; then
                LAST_SEQUENCE="$VALUE"
              fi
            fi

            NEXT_SEQUENCE=$((LAST_SEQUENCE + 1))
            while [ -d "$HISTORY_ROOT/$NEXT_SEQUENCE" ]; do
              NEXT_SEQUENCE=$((NEXT_SEQUENCE + 1))
            done

            printf "%s\n" "$NEXT_SEQUENCE" > "$SEQUENCE_FILE"
            echo "REPORT_SEQUENCE=$NEXT_SEQUENCE" >> "$GITHUB_ENV"
            echo "Using report sequence $NEXT_SEQUENCE for scope $REPORT_SCOPE and name $REPORT_NAME."
        shell: bash

      - name: Generate Allure report
        if: always()
        run: |
          set -euo pipefail
          DEVICE="${{ inputs.device }}"
            REPORT_SEQUENCE="${REPORT_SEQUENCE:?REPORT_SEQUENCE not set}"
            REPORT_NAME="${{ env.REPORT_NAME }}"
            RESULTS_DIR="allure-results/$REPORT_SCOPE"
            HISTORY_SNAPSHOT="allure-history/$REPORT_SCOPE/$REPORT_NAME/latest/history"

            if [ ! -d "$RESULTS_DIR" ] || [ -z "$(ls -A "$RESULTS_DIR" 2>/dev/null)" ]; then
              echo "No allure results found for $DEVICE. Skipping report generation."
              exit 0
            fi

            if [ -d "$HISTORY_SNAPSHOT" ]; then
              mkdir -p "$RESULTS_DIR/history"
              rsync -a --delete "$HISTORY_SNAPSHOT/" "$RESULTS_DIR/history/"
            fi

            OUTPUT_DIR="allure-history/$REPORT_SCOPE/$REPORT_NAME/$REPORT_SEQUENCE"
            allure generate "$RESULTS_DIR" --clean -o "$OUTPUT_DIR"
            rm -rf "allure-history/$REPORT_SCOPE/$REPORT_NAME/latest"
            cp -R "$OUTPUT_DIR" "allure-history/$REPORT_SCOPE/$REPORT_NAME/latest"
        shell: bash

      - name: Create redirect files
        if: always()
        run: |
          set -euo pipefail
          REPORT_SCOPE="${{ env.REPORT_SCOPE }}"
          REPORT_NAME="${{ env.REPORT_NAME }}"
          REPORT_SEQUENCE="${{ env.REPORT_SEQUENCE }}"
          REPORT_NAME_REDIRECT_FILE="allure-history/$REPORT_SCOPE/$REPORT_NAME/index.html"
          echo "<!DOCTYPE html><html><head><meta http-equiv=\"refresh\" content=\"0; url=./$REPORT_SEQUENCE/\"></head><body><p>Redirecting to the <a href=\"./$REPORT_SEQUENCE/\">latest report for $REPORT_NAME</a>.</p></body></html>" > "$REPORT_NAME_REDIRECT_FILE"
          REPORT_SCOPE_REDIRECT_FILE="allure-history/$REPORT_SCOPE/index.html"
          echo "<!DOCTYPE html><html><head><meta http-equiv=\"refresh\" content=\"0; url=./$REPORT_NAME/$REPORT_SEQUENCE/\"></head><body><p>Redirecting to the <a href=\"./$REPORT_NAME/$REPORT_SEQUENCE/\">latest report for the $REPORT_SCOPE scope</a>.</p></body></html>" > "$REPORT_SCOPE_REDIRECT_FILE"
        shell: bash

      - name: Generate portal page
        if: always()
        run: |
          node .github/scripts/generate-portal.js
        shell: bash

      - name: Ensure Jekyll is disabled
        if: always()
        run: |
          touch allure-history/.nojekyll
        shell: bash

      - name: Publish test report
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-history
          force_orphan: true

      - name: Post the link to the report
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TARGET_URL: ${{ env.REPORT_BASE_URL }}/${{ env.REPORT_SCOPE }}/${{ env.REPORT_NAME }}/${{ env.REPORT_SEQUENCE }}
        run: |
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            /repos/${{ github.repository }}/statuses/${{ github.sha }} \
            -f state='success' \
            -f target_url="$TARGET_URL" \
            -f context='Test report'

      - name: Send failure email
        if: always()
        run: |
          echo "Starting email script..."
          node .github/scripts/send-email.js
        env:
          SMTP_USER: ${{ secrets.SMTP_USER_NASKO }}
          SMTP_PASS: ${{ secrets.SMTP_PASS_NASKO }}
          SMTP_HOST: ${{ secrets.SMTP_HOST_NASKO }}
          SMTP_PORT: ${{ secrets.SMTP_PORT_NASKO }}
          TARGET_URL: ${{ env.REPORT_BASE_URL }}/${{ env.REPORT_SCOPE }}/${{ env.REPORT_NAME }}/${{ env.REPORT_SEQUENCE }}